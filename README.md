# JSON Style Viewer in React/Redux

To get the webserver started type: 

```bash
$ npm install
$ DEBUG=* npm start
```

Once the server is running, you can get instructions for the challenge at http://localhost:3000.
